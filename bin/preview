#!/usr/bin/env bash

set -euo pipefail

docker run --rm -it \
  -p 4000:4000 \
  -v "$PWD:/srv/jekyll" \
  -v "$PWD/.bundle:/usr/local/bundle" \
  jekyll/jekyll:pages \
  sh -lc 'bundle install && bundle exec jekyll serve --livereload --host 0.0.0.0 --config _config.yml,_config.local.yml'
